/* ==========================================================================
   Claude Monitor — Cost Page (placeholder)
   ========================================================================== */

const CostPage = {
    render(container) {
        container.innerHTML = `
            <h2>Cost</h2>
            <p class="text-muted">Coming soon...</p>
        `;
    },
};
