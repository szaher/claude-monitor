/* ==========================================================================
   Claude Monitor — Settings Page (placeholder)
   ========================================================================== */

const SettingsPage = {
    render(container) {
        container.innerHTML = `
            <h2>Settings</h2>
            <p class="text-muted">Coming soon...</p>
        `;
    },
};
