/* ==========================================================================
   Claude Monitor — Sessions Page (placeholder)
   ========================================================================== */

const SessionsPage = {
    render(container) {
        container.innerHTML = `
            <h2>Sessions</h2>
            <p class="text-muted">Coming soon...</p>
        `;
    },
};
