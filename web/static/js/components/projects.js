/* ==========================================================================
   Claude Monitor — Projects Page (placeholder)
   ========================================================================== */

const ProjectsPage = {
    render(container) {
        container.innerHTML = `
            <h2>Projects</h2>
            <p class="text-muted">Coming soon...</p>
        `;
    },
};
