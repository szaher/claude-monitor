/* ==========================================================================
   Claude Monitor — Agents Page (placeholder)
   ========================================================================== */

const AgentsPage = {
    render(container) {
        container.innerHTML = `
            <h2>Agents</h2>
            <p class="text-muted">Coming soon...</p>
        `;
    },
};
