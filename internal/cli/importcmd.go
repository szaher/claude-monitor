package cli

import "fmt"

// Import imports historical Claude Code session logs into the database.
// The function is named Import (not "import") to avoid the Go keyword.
func Import(args []string) error {
	fmt.Println("Command 'import' not yet implemented")
	return nil
}
